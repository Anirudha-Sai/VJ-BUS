<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PERMISSIONS FOR FOREGROUND SERVICE & BOOT -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/> <!-- ADDED: Required for autoStartOnBoot -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- PERMISSIONS FOR ANDROID 13+ NOTIFICATIONS -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/> <!-- ADDED: Critical for foreground service notification -->

    <!-- PERMISSIONS FOR LOCATION -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> <!-- ADDED: Crucial for location when app is closed -->
    
    <!-- GENERAL PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- REMOVED: `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` is handled programmatically. -->
    
    <application
        android:label="VJ Bus Driver"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- REMOVED: `usesCleartextTraffic` is not needed for secure websocket (wss://) -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- CORRECTED SERVICE DECLARATION -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="location" />
        <!-- REMOVED: `android:permission="android.permission.BIND_JOB_SERVICE"` is not correct for this package. -->

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <!-- REMOVED: The <queries> tag is likely not needed for your use case and can be removed for simplicity. -->
</manifest>
